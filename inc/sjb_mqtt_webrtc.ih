#pragma pack(push, 4)

STRUCT(gsf_mqtt_webrtc_cfg_t)
{
  FIELD(int, port);
  FIELD(int, auth);
  FIELD(int, webrtc_en);  // 1: enable conncet public webrtc server;
  STRING(mqtt_sever_url,256); //"wss://openhisilicon.glitch.me/hiview-camera"
};

#pragma pack(pop)